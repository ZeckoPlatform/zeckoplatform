import express from 'express';
const router = express.Router();

// Placeholder for subscription routes - to be implemented
router.get('/subscription/status', (req, res) => {
  res.json({ status: 'active' });
});

export default router;
