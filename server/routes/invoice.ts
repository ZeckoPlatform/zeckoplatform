import express from 'express';
const router = express.Router();

// Placeholder for invoice routes - to be implemented
router.get('/invoice/list', (req, res) => {
  res.json({ invoices: [] });
});

export default router;
