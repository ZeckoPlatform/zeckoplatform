import express from 'express';
const router = express.Router();

// Placeholder for review routes - to be implemented
router.get('/review/list', (req, res) => {
  res.json({ reviews: [] });
});

export default router;
