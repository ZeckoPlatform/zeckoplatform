import express from 'express';
const router = express.Router();

// Placeholder for order routes - to be implemented
router.get('/order/list', (req, res) => {
  res.json({ orders: [] });
});

export default router;
