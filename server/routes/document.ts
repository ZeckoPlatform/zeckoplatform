import express from 'express';
const router = express.Router();

// Placeholder for document routes - to be implemented
router.get('/document/list', (req, res) => {
  res.json({ documents: [] });
});

export default router;
