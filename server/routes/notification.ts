import express from 'express';
const router = express.Router();

// Placeholder for notification routes - to be implemented
router.get('/notification/list', (req, res) => {
  res.json({ notifications: [] });
});

export default router;
